#!/bin/sh
set -e

MPI_SELECTOR=`which mpi-selector` || true

if [ ! -z "$MPI_SELECTOR" ] ; then
	mpi-selector --unregister openmpi_gcc_hfi-2.1.2 --yes || \
		true >/dev/null 2>/dev/null
fi

#DEBHELPER#
